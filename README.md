This involves using terraform to create VPC.
First i started out by installing the necessary things like terraform using chocolatey and installing aws cli. 
I proceeded to VS code editor to create my files and writing the code needed. I created the VPC while defining the resource, created the public and private subnets, route tables, NAT gateways and linking them all together while looking up at the terraform documentary.
The next step was to create a key which is used and then running on bash
I ran terraform init to initialize terraform and then to get the output files 
The next step was to create the repo and creating a read me file and finally pushing to guthub

